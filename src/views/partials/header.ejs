<nav class="navbar navbar-expand-lg navbar-custom">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="/">PERSÉ ERP</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Menú de usuario para pantallas pequeñas -->
            <% if (user) { %>
                <div class="dropdown user-menu ms-auto d-lg-none mb-3">
                    <a class="d-flex align-items-center nav-link dropdown-toggle" href="#" id="userMenuLinkMobile" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <% if (user.photoUrl) { %>
                            <img src="<%= user.photoUrl %>" alt="Foto de perfil">
                        <% } else { %>
                            <div class="bg-light text-dark d-flex justify-content-center align-items-center rounded-circle" style="width: 30px; height: 30px;">
                                <%= user.initials %>
                            </div>
                        <% } %>
                        <span class="ms-2 d-inline d-md-none"><%= user.Nombre %></span>
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="userMenuLinkMobile">
                        <li><a class="dropdown-item" href="/logout">Cerrar sesión</a></li>
                    </ul>
                </div>
            <% } %>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="comercialMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Comercial
                    </a>
                    <ul class="dropdown-menu submenu" aria-labelledby="comercialMenuLink">
                        <li><a class="dropdown-item" href="/listado-facturas" data-content="facturacion">Facturación Electrónica</a></li>
                        <li><a class="dropdown-item" href="#" data-content="gestionclientes">Gestión de Clientes</a></li>
                        <li><a class="dropdown-item" href="#" data-content="anticipoclientes">Anticipos de Clientes</a></li>
                        <li><a class="dropdown-item" href="#" data-content="cartera">Cartera y Cobranza</a></li>
                        <li><a class="dropdown-item" href="#" data-content="controlrentabilidad">Control de Rentabilidad</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="proveedoresMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Proveedores
                    </a>
                    <ul class="dropdown-menu submenu" aria-labelledby="proveedoresMenuLink">
                        <li><a class="dropdown-item" href="#" data-content="compraselectronicas">Compras Electrónicas</a></li>
                        <li><a class="dropdown-item" href="#" data-content="anticiposproveedores">Anticipos de Proveedores</a></li>
                        <li><a class="dropdown-item" href="#" data-content="cuentasxpagar">Cuentas por Pagar</a></li>
                        <li><a class="dropdown-item" href="#" data-content="inventario">Inventario</a></li>
                    </ul>
                </li>
            </ul>
            <!-- Menú de usuario para pantallas grandes -->
            <% if (user) { %>
                <div class="dropdown user-menu ms-auto d-none d-lg-block">
                    <a class="d-flex align-items-center nav-link dropdown-toggle" href="#" id="userMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <% if (user.photoUrl) { %>
                            <img src="<%= user.photoUrl %>" alt="Foto de perfil" style="width: 30px; height: 30px; border-radius: 50%;">
                        <% } else { %>
                            <div class="bg-light text-dark d-flex justify-content-center align-items-center rounded-circle" style="width: 30px; height: 30px;">
                                <%= user.initials %>
                            </div>
                        <% } %>
                        <span class="d-none d-md-inline ms-2"><%= user.Nombre %></span>
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="userMenuLink">
                        <li><a class="dropdown-item" href="/logout">Cerrar sesión</a></li>
                    </ul>
                </div>
            <% } %>
        </div>
    </div>
</nav>
